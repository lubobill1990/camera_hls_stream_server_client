# =========================================
# HLS Camera Streaming Server Configuration
# =========================================

# Server Configuration
# --------------------

# Port the server listens on
# Default: 3001
PORT=3001

# FFmpeg Configuration
# --------------------

# Path to FFmpeg executable
# Leave empty to use FFmpeg from system PATH
# Examples:
#   Windows: C:\ffmpeg\bin\ffmpeg.exe
#   macOS:   /usr/local/bin/ffmpeg
#   Linux:   /usr/bin/ffmpeg
FFMPEG_PATH=

# HLS Output Configuration
# ------------------------

# Directory for HLS output files (playlists and segments)
# Can be relative to server root or absolute path
# Default: ./public/hls
HLS_OUTPUT_DIR=./public/hls

# Duration of each HLS segment in seconds
# Lower values = lower latency, higher values = more stable
# Recommended: 2-4 seconds
# Default: 2
HLS_SEGMENT_DURATION=2

# Number of segments to keep in the playlist
# Higher values = longer DVR window, more disk usage
# DVR window = HLS_SEGMENT_DURATION * HLS_LIST_SIZE
# Default: 10 (20 seconds with 2s segments)
HLS_LIST_SIZE=10

# Minimum disk space required to start streaming (MB)
# Default: 500
MIN_DISK_SPACE_MB=500
